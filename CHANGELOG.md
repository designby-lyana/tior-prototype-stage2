# Changelog — TIOR / OponyToMy (Stage 2)

Формат: дата → зміна → commit.

## 2026-08-04

- **Wyniki wyszukiwania (mobile), картка №2:** ще 3 правки. (1) Прибрано окрему (i)-іконку тултіпа — тап тепер на самому лейблі ціни (`.pr-price-b2b-tap`, пунктирне підкреслення замість іконки як натяк на інтерактивність); заразом виправлено баг, який ця зміна могла внести — клік по вмісту вже відкритого тултіпа (тултіп тепер вкладений у клікабельний лейбл) більше не закриває його одразу (`event.stopPropagation()` на `.pr-price-b2b-tip`). (2) Тултіпи польською: "Cena bazowa brutto/netto", "Rabat", "Twoja cena brutto/netto (-5%)" для "Twoja cena"; "Cena zakupu", "Marża", "Cena sprzedaży brutto/netto" для "Cena sprzedaży" (було англійською). (3) Мінімально зменшено відступ Naличність→Доставка ЛИШЕ на картці №2 (`.ref-card-twin .pr-delivery{margin-top:2px}`, було var(--space-xs)=4px; картка №1/решта карток без змін).
- **Wyniki wyszukiwania (mobile), картка №2:** 3 правки за фідбеком. (1) Приховано netto-ціни на обох плитках ("Twoja cena" і "Cena sprzedaży") — лишили тільки brutto. Заодно виправлено ту саму помилку переплутаних класів/значень на "Cena sprzedaży" (за аналогією з "Twoja cena" минулого разу): brutto €114.17 тепер перше/болдом (pr-gross), netto €92.82 — приховане (pr-netto); tooltip-рядок "Selling gross" тепер strong замість "Selling net". (2) Прибрано `white-space:nowrap` з лейблів цін ("Cena sprzedaży" + іконка i розпирали price-col понад max-width:112px, через що "225/45 R17 94Y" зліва переносився на 2 рядки) — лейбл тепер сам переноситься за потреби, не ламаючи розмір шини. (3) Гарантовано мінімальний відступ між блоком цін і Naличність/Доставка через `.pr-price-col-b2b{ justify-content:flex-start; gap:var(--space-md) }` замість `justify-content:space-between` у фіксованому 120px-боксі, який на вищому B2B-блоці стискався мало не до нуля. addToCart тепер рахує суму від Selling gross (€114.17) — коректніше, ніж netto, бо це фактична сума з ПДВ, яку платить клієнт.
- **Fix:** тултіп "Twoja cena" на картці №2 — рядки типу "Your price brutto (-5%)" переносились на 2 рядки (min-width:200px замалий). Тепер `.pr-price-b2b-tip` на `width:max-content` (+ `max-width:calc(100vw - 48px)` як запобіжник від виходу за екран) і `white-space:nowrap` на рядках — ширина підлаштовується під найдовший рядок, перенос неможливий.
- **Wyniki wyszukiwania (mobile), картка №2, "Twoja cena":** вища ціна (brutto, €99.27) тепер перша й болдом, нижча (netto, €80.71) — друга й сірим (раніше було навпаки, і класи .pr-gross/.pr-netto були переплутані зі значеннями). Тултіп розширено до 5 рядків: Base price brutto, Base price netto, Discount, роздільник, Your price brutto (-5%), Your price netto (-5%) — замість попередніх 4 рядків з одним "Gross price" наприкінці.
- **Wyniki wyszukiwania (mobile), картка №2:** впроваджено B2B-ціни — "Twoja cena" (закупівельна зі знижкою 5%) і "Cena sprzedaży" (продажна з маржею 15%) замість одного .pr-price. Видно лише залогованому (guest — без змін, той самий замок). (i)-іконка біля кожної ціни відкриває тултіп-розрахунок по тапу (Base price → Discount → Your price → Gross; Purchase price → Margin → Selling net → Selling gross). Глобальний тумблер "Ukryj cenę" у панелі фільтрів (поруч з EAN) ховає "Twoja cena" на всіх картках з новим блоком (поки — лише №2) і спрощує заголовок "Cena sprzedaży" → "Cena". Кошик ("Dodaj") далі рахує суму від "Cena sprzedaży" (те, що клієнт платить продавцю), а не від власної закупівельної ціни продавця. Усе позначено NOWA FUNKCJA/KONIEC NOWEJ FUNKCJI, перевірено jsdom-симуляцією (guest/logged, тултіпи, тумблер, кошик, пагінація/пошук — щоб не повторити баг картки №2 з попереднього кроку).
- **Fix:** картка №2 (дублікат) з'являлась одразу при вході на екран результатів, хоча мала бути прихована до кліку. Причина: приховування було на inline `style="display:none"`, а `runResultsSearch()` (запускається автоматично при кожному вході на screenResults через `resetResultsPagination()`) явно скидає `style.display=''` для ВСІХ `.product-row` — це й "розкривало" картку №2 без кліку. Також id="refCardTwin" не працював як довгостроковий якір — пагінація (`buildResultsCard`) клонує картки з пулу і зрізає `id` з кожного клону. Виправлено: видимість тепер керується класом `.ref-card-twin` / `.is-revealed` (CSS, `!important`) замість inline style, а JS шукає картку №2 як `.nextElementSibling` клікнутої картки №1 (із запасним варіантом через `.querySelector`) замість `getElementById`. Лічильник "N produktów" (`updateFilterState`) також оновлено, щоб не рахувати приховану картку №2. Перевірено симуляцією кліків + повторного входу на екран (jsdom).
- **Wyniki wyszukiwania (mobile):** картка №2 — дублікат еталонної картки №1 (Nokian), прихована за замовчуванням, з'являється одразу під карткою №1 по кліку на іконки "авто + сезон" в картці №1 (toggle, повторний клік ховає назад). Новий JS `toggleRefCardTwin()` і атрибути на `.pr-attrs-left` позначено коментарями `NOWA FUNKCJA`.
- **Wyniki wyszukiwania (mobile):** додано ЕТАЛОННУ картку товару (Product Card v2, затверджений вигляд) першою карткою в список результатів пошуку. Джерело макету — файл "product card 3 breakpoints.html". Нова розмітка йде під класами `.pr-redesign`/`.pr-font-v2`, весь новий код позначено коментарями `NOWA FUNKCJA`/`KONIEC NOWEJ FUNKCJI` в HTML і CSS — щоб відрізнити від решти карток, які поки лишаються в попередньому стилі. Додано 4 нові CSS-токени (`--radius-md`, `--radius-xs-v2`, `--color-season-summer`, `--color-season-winter`), решта карток і базові стилі не змінювались.

## 2026-07-28

- **Чекаут (Podsumowanie zamówienia):** прибрано чекбокс "Potwierdzam szczegóły zamówienia i akceptuję warunki." Замінено на дрібний текст під кнопкою "Złóż zamówienie" — той самий патерн, що й на формі "Skontaktuj się": "Składając zamówienie, akceptujesz Regulamin i Politykę prywatności OponyToMy." (commit `f4ee0e6`)
